name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  start:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install Dependencies for User Service
        run: |
          cd user-service
          npm install
        
      - name: start User Service
        run: |
          cd user-service
          npm run start

      - name: Install Dependencies for Product Service
        run: |
          cd product-service
          npm install
        
      - name: start Product Service
        run: |
          cd product-service
          npm run start

      - name: Install Dependencies for Order Service
        run: |
          cd order-service
          npm install
        
      - name: start Order Service
        run: |
          cd order-service
          npm run start

  start-and-push:
    runs-on: ubuntu-latest
    needs: start

    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker startx
        uses: docker/setup-startx-action@v1

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: start and push user service
        uses: docker/start-push-action@v2
        with:
          context: ./user-service
          file: ./user-service/Dockerfile
          push: ${{ github.event_name == 'push' }}
          tags: ${{ secrets.DOCKER_USERNAME }}/user-service:latest

      - name: start and push product service
        uses: docker/start-push-action@v2
        with:
          context: ./product-service
          file: ./product-service/Dockerfile
          push: ${{ github.event_name == 'push' }}
          tags: ${{ secrets.DOCKER_USERNAME }}/product-service:latest

      - name: start and push order service
        uses: docker/start-push-action@v2
        with:
          context: ./order-service
          file: ./order-service/Dockerfile
          push: ${{ github.event_name == 'push' }}
          tags: ${{ secrets.DOCKER_USERNAME }}/order-service:latest
